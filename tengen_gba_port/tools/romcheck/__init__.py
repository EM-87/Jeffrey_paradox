"""The headless checks `make gba-check` runs against the built ROM.

tools/run_rom.py is the command; this package is what it runs. harness.py
is the shared machinery, and every other module is one family of checks.
"""
